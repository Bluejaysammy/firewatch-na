# ============================================================
# FireWatch NA — environment configuration
# Copy to .env.local for development (or set in your host).
# The app works fully WITHOUT any keys: all core data sources
# (WFIGS, CWFIS, NWS, NOAA, Open-Meteo, RainViewer, OSM) are
# public and keyless. Keys below unlock optional extra layers.
# ============================================================

# --- Optional integrations (extra map layers) ---

# NASA FIRMS hotspots (replaces the CWFIS hotspot feed with FIRMS VIIRS).
# Free key: https://firms.modaps.eosdis.nasa.gov/api/
FIRMS_MAP_KEY=

# OpenWeatherMap tile layers (wind, temperature, precipitation).
# Free key: https://openweathermap.org/api
OWM_API_KEY=

# World Air Quality Index tile layer (ground-station AQI overlay).
# Free token: https://aqicn.org/data-platform/token/
WAQI_TOKEN=

# --- Site identity ---

# Public URL of your deployment (used for canonical links, sitemap, OG tags).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Tuning (defaults shown) ---

# Default client auto-refresh interval, seconds (user-adjustable in the UI).
REFRESH_DEFAULT_SECONDS=300

# Server-side cache TTLs, seconds.
FIRES_TTL_SECONDS=120
PERIMETERS_TTL_SECONDS=300
HOTSPOTS_TTL_SECONDS=300
ALERTS_TTL_SECONDS=120
ROADS_TTL_SECONDS=600
CLOSURES_TTL_SECONDS=300
EVAC_TTL_SECONDS=300
CAMERAS_TTL_SECONDS=900

# Add more official 511/Open511 road-event feeds without code changes:
# a JSON array of {"id","label","type":"v2"|"open511","url"} objects, e.g.
# EXTRA_511_SOURCES=[{"id":"mn-511","label":"Minnesota 511","type":"v2","url":"https://511mn.org/api/v2/get/event"}]
EXTRA_511_SOURCES=

# Identify your deployment to upstream data providers (please customize;
# Nominatim's usage policy requires a valid contact).
UPSTREAM_USER_AGENT="FireWatch-NA/1.0 (your-contact@example.com)"

# Set to 1 only for load testing; leaves API routes unthrottled.
RATE_LIMIT_DISABLED=

# --- Community accounts & reports ---

# Where the SQLite database and uploaded photos live. MUST be a persistent
# volume in production (Render: attach a disk at /data; Docker: mount a
# volume) or accounts/reports vanish on redeploy.
DATA_DIR=data

# Comma-separated usernames that get moderator powers (hide/remove any report).
ADMIN_USERNAMES=

# Community reports disappear from the map after this many days (stale
# sighting reports are misinformation).
REPORT_MAX_AGE_DAYS=7
